# npm dependencies are bumped manually (exact pins in package.json + committed lockfile).
# Only GitHub Actions versions are proposed automatically.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    open-pull-requests-limit: 3
    labels:
      - dependencies
      - github-actions
